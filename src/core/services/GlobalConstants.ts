
//
enum  SYMBL_INSIGHT_TYPES {
    ACTION_ITEM = 'action_item',
    QUESTION='question'
}
// By annotating an enum option, you set the value;
// increments continue from that value:
enum StatusCodes {
    OK = 200,
    BadRequest = 400,
    Unauthorized,
    PaymentRequired,
    Forbidden,
    NotFound,
}